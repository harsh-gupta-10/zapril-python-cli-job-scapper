[project]
name = "job-scapper"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "beautifulsoup4",
    "geopy",
    "pandas",
    "playwright",
    "python-jobspy",
    "rapidfuzz",
    "requests",
    "rich",
]
